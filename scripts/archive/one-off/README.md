# Archived one-off scripts

These scripts were used for **one-time migrations**, **encoding repairs** (mojibake / emoji), **debug dumps**, or **local experiments**. They are **not** part of normal install, deploy, or test workflows.

**Do not run** unless you understand what each script does against your current tree and database.

Supported maintenance scripts remain in the parent **`scripts/`** folder (see root **`README.md`** → Scripts).
