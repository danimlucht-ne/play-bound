# Kiro

Feature specifications that previously lived under **`.kiro/specs/`** were **archived** to:

**`docs/archive/kiro-specs/`**

New Kiro work can recreate **`.kiro/specs/<feature>/`** if you use Kiro again; this stub avoids an empty-looking `.kiro` root with no explanation.
